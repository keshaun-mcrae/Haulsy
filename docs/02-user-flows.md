# User Flows

## Seller
Create listing ? Publish ? Chat ? Propose meetup time ? Mark sold

## Buyer
Browse ? Open listing ? Message ? Schedule pickup

## Translation
Open chat ? Tap ?? Translate toggle ? See translated messages ? Tap “View original” per message

## Cars
Cars listing uses structured fields (Make/Model/Year/KM/Transmission)
