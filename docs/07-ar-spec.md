# AR “Will it fit?” Spec

## Release ladder
- v0: require dimensions (L/W/H) for furniture listings + fit helper UI
- v1: AR bounding box placement behind feature flag
- v2: optional 3D models

## Notes
AR is not MVP. Build only after core marketplace + chat is stable.
